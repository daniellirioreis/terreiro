<table class="table">
  <td>
    <h3>Listagem de notícias</h3>
  </td>
  <td width='5%'>
    <%= link_to new_reminder_path, role: 'button' do %>
      <h2 class="text-success" title='Nova Notícia'>+</h2>
    <% end %>    
  </td>
</table>

<%= label_tag 'txtBusca', 'Pesquisar' %>
<%= text_field_tag 'txtBusca', '', :class => 'span11 input-search', :id => 'txtBusca',:placeholder => "Buscar nesta lista", :alt =>"list_reminders" %>

<table class="table list_reminders">
  <thead>
    <tr>
      <th>Título</th>
      <th>Descrição</th>
      <th colspan="3" width="5%"></th>
    </tr>
  </thead>

  <tbody>
    <% @reminders.each do |m| %>
      <tr>
        <td><%= m %></td>
        <td><%= m.description %></td>
        <td>
          <%= link_to edit_reminder_path(m), role: 'button' do %>
            <strong>Editar</strong>
          <% end %>
        </td>
        <td>
          <%= link_to m, method: :delete, data:{confirm: 'Confirma exclusão ?'}, role: 'button' do %>
            <strong>Apagar</strong>
          <% end %>              
        </td>
        <td>
          <%= link_to m, role: 'button' do %>
            <strong>Info</strong>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
