<%= simple_form_for(@patient) do |f| %>
  <%= hidden_field_tag :from_where, @from_where %>
  <div class="tabbable">
    <ul class="nav nav-tabs">
      <li class="active"> <%= link_to "Geral", "#main", :"data-toggle" => :tab %></li>
      <li> <%= link_to "Endereço", "#address", :"data-toggle" => :tab %></li>
    </ul>
  </div>
  <div class="tab-content">
    <div class="tab-pane fade active in" id="main">
      <%= render "main", :f => f %>
    </div>
    <div class="tab-pane" id="address">
      <%= render "address", :f => f %>
    </div>
  </div>
  <div class="form-actions">
    <%= f.button :submit, :class => "btn btn-success" %>
    <% if  params[:from_where] == "scheduling"%>
      <%= link_to 'Voltar', scheduling_index_path, :class => "btn"  %>
    <% else %>
      <%= link_to 'Voltar', patients_path, :class => "btn"  %>
    <% end %>
    
  </div>

<% end %>
